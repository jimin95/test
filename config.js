/* We will need to create MongoDB databases. */

module.exports = {
    //secret: used when we create and verify JSON Web Tokens
    'secret': 'secretkeyPwd',
    //database: the URI with username and password to your MongoDB installation
    'database': 'mongodb://localhost:noderauth&54;proximus.modulusmongo.net:27017/so9pojyN'
}